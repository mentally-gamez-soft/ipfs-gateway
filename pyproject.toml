[project]
name = "ipfs-gateway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pip==25.3",
    "pre-commit==4.5.1",
    "Flask==3.0.0",
    "python-dotenv==1.0.1",
    "pytest==8.0.0",
]

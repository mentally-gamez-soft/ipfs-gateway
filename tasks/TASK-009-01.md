# TASK-009-01: Celery/Redis configuration

- Story: US-009
- Estimate: 8h
- Status: ✅ Completed
- Actual Effort: 8h
- Dependencies: TASK-001-04

## Description
Configure Celery with Redis broker/result. Create `core/celery_app.py` initializing Celery with Flask context, JSON serializer, and default retry/backoff settings. Add worker startup docs.

## Acceptance Criteria
- ✅ `celery -A core.celery_app worker` starts and discovers tasks
- ✅ Redis broker/result configured via env (CELERY_BROKER_URL, CELERY_RESULT_BACKEND)
- ✅ Tasks can access DB and config via app context
- ✅ Docker Compose with Redis service configured
- ✅ Task routing set up for upload and pin queues

## Implementation Details

### Files Created/Modified
- `docker-compose.yml` - Redis 7 container on port 6379
- `core/celery_app.py` - Celery app factory with JSON serialization
- `.env` - Added CELERY_BROKER_URL, CELERY_RESULT_BACKEND
- `pyproject.toml` - Added celery>=5.4.0, redis>=5.2.0

### Configuration
- Broker: `redis://localhost:6379/0`
- Result Backend: `redis://localhost:6379/0`
- Serializer: JSON (for Celery compatibility)
- Task Acks: Late (acknowledge after completion)
- Track Started: Yes
- Max Retries: 3
- Result Expiry: 3600s (1 hour)
- Queues: default, upload (routing_key: upload.#), pin (routing_key: pin.#)

### Worker Startup
```bash
./start_worker.sh  # Launches worker with --beat, 4 concurrency
```

## References
- [US-009: Asynchronous Tasks (Celery + Redis)](user-stories/US-009-async-celery-redis.md)
